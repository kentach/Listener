<% if booklist %>
  <%= button_to "マイテキストから削除",
      booklist_path(booklist),
      method: :delete,
      class: "my-list-btn active" %>
<% else %>
  <%= button_to "マイテキストに追加",
      booklists_path(textbook_id: textbook.id),
      method: :post,
      class: "my-list-btn" %>
<% end %>